import { ProductListDisplay } from './product-list-display';

describe('ProductListDisplay', () => {
  it('should create an instance', () => {
    expect(new ProductListDisplay()).toBeTruthy();
  });
});
